swagger: '2.0'
info:
  title: Student Application Decision Web API
  description: Web API for creating, reading, updating, and deleting student application decision codes.
  version: '0.0'
externalDocs:
  description: GitHub Repository
  url: https://github.com/osu-mist/sad-api
host: localhost
schemes:
  - http
paths:
  /:
    get:
      summary: Sample Resource
      description: This is a sample resource that returns a short message.
      responses:
        200:
          description: A short message
          schema:
            type: string
    post:
    put:
    delete:
  /{pidm}:
    get:
      summary: Get all
      description: Get all student application decisions for a given pidm
      responses:
        200:
          description: ok
          schema:
            type: application/json
